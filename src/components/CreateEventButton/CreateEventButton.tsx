import React from 'react';

const CrearEventoButton = () => {
  return (
    <button className="bg-teal-400 text-white text-xs py-2 px-4 rounded-full flex items-center">
      <p>Crear evento</p>
    </button>
  );
};

export default CrearEventoButton;
